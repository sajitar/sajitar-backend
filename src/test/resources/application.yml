# Mesma cadeia que ambiente local (local.env: functions → Hibernate DDL → columns, uniques, indexes, settlement)
spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:util/functions.sql
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          '[import_files]': util/columns.sql, util/uniques.sql, util/indexes.sql, settlement/profile.sql, settlement/checker.sql, settlement/authority.sql
sajitar:
  domain:
    validation:
      profile:
        birthday:
          min-age-years: 18
      limit:
        max: 100
